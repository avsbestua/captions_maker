# CapTer🔠
This app makes captions in offline
## Stack✅
* OpenAI Whisper
* MoviePy
* Tkinter

# Status 🪫
CapTer is currently under development. So the program does not have many features yet.

# Platforms ⚠️
Currently, tested only on Mac (arm). Windows compatibility not guranted

# Instalation 💻
**Currenly this program avaible only on mac. In future it will be for Windows too**

## MacOS🍏
First, you need to install brew. Run this in terminal

```
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```
Then install ffmpeg using brew
```
brew install ffmpeg
```
